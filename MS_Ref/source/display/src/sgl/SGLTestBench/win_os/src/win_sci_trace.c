#include <stdio.h>
#include "win_sci_trace.h"

PUBLIC uint32 SCI_TraceLow(const char *x_format, ...)
{
    //printf(x_format);    
}

PUBLIC uint32 SCI_Trace(const char *x_format, ...)
{
    //printf(x_format);    
}

PUBLIC void SCI_TraceEx(uint32 trc_class,  const char* format, const uint8 * arg_type,...)
{
    
}